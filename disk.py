class Disk:
    def __init__(self, width):
        self.width = width
        
    def body(width):
        a = [1] * width
        while len(a) < 5:
            a.insert(0, 0)
            a.append(0)

        return a[0:5]
